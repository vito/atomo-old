data bool: true | false
data card: king | queen | jack | ace | joker

bool isKing(card a):
    if a == king:
        true
    else:
        false

dump(isKing(joker))
dump(isKing(ace))
dump(isKing(jack))
dump(isKing(queen))
dump(isKing(king))
